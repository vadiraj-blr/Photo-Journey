// Vercel serverless entry (used when the Vercel project Root Directory is
// set to artifacts/wildpixels). Wraps the shared Express app.
import app from "../../api-server/src/app";

export default app;
