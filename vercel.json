{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "installCommand": "pnpm install",
"buildCommand": "BASE_PATH=/ pnpm --filter @workspace/wildpixels run build && pnpm --filter @workspace/api-server run build && node scripts/prerender-seo.mjs",
  "outputDirectory": "artifacts/wildpixels/dist/public",
  "rewrites": [
    {
      "source": "/api/:path*",
      "destination": "/api/index"
    },
    {
      "source": "/:path*",
      "destination": "/index.html"
    }
  ],
  "framework": null
}
